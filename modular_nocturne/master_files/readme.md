This folder mirrors the file path of the base repo. For example, /modular_nocturne/master_files/code, /modular_nocturne/master_files/icons/, etc.

Only use this folder if something from upstream needs to be completely overridden.
